---
interface Props {
  class?: string
  pagefindBody?: boolean
}

const { class: className = "", pagefindBody = false } = Astro.props
const pagefindAttrs = pagefindBody ? { "data-pagefind-body": "" } : {}
---

<div
  class={`content-prose prose prose-neutral dark:prose-invert ${className}`}
  {...pagefindAttrs}
>
  <slot />
</div>
